description: >
  Load the version into an environment variable
  using the value from package.json


steps:
  - run:
      name: Loading VERSION
      command: << include(scripts/js/export-version.sh) >>
